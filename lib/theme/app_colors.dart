import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ─── Brand Core ──────────────────────────────────────────────────────────
  static const Color primaryNavy = Color(0xFF0B1D3A);
  static const Color primaryBlue = Color(0xFF1A56DB);
  static const Color primaryBlueMid = Color(0xFF2563EB);
  static const Color primaryBlueLight = Color(0xFF3B82F6);
  static const Color accentTeal = Color(0xFF0D9488);
  static const Color accentTealLight = Color(0xFF14B8A6);
  static const Color accentCyan = Color(0xFF06B6D4);

  // ─── Neutrals ────────────────────────────────────────────────────────────
  static const Color white = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF8FAFF);
  static const Color surfaceElevated = Color(0xFFFFFFFF);
  static const Color surfaceMuted = Color(0xFFF1F5F9);
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderLight = Color(0xFFF1F5F9);
  static const Color divider = Color(0xFFE8EDF5);

  // ─── Text ────────────────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFF0B1D3A);
  static const Color textSecondary = Color(0xFF475569);
  static const Color textMuted = Color(0xFF94A3B8);
  static const Color textInverse = Color(0xFFFFFFFF);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // ─── Semantic ────────────────────────────────────────────────────────────
  static const Color success = Color(0xFF10B981);
  static const Color successLight = Color(0xFFD1FAE5);
  static const Color warning = Color(0xFFF59E0B);
  static const Color warningLight = Color(0xFFFEF3C7);
  static const Color error = Color(0xFFEF4444);
  static const Color errorLight = Color(0xFFFEE2E2);
  static const Color info = Color(0xFF3B82F6);
  static const Color infoLight = Color(0xFFDBEAFE);

  // ─── Premium / Monetization ──────────────────────────────────────────────
  static const Color premiumGold = Color(0xFFF59E0B);
  static const Color premiumGoldLight = Color(0xFFFEF3C7);
  static const Color premiumPurple = Color(0xFF7C3AED);
  static const Color premiumPurpleLight = Color(0xFFEDE9FE);

  // ─── Career Category Colors ───────────────────────────────────────────────
  static const Color careerTech = Color(0xFF1A56DB);
  static const Color careerMedicine = Color(0xFF10B981);
  static const Color careerBusiness = Color(0xFFF59E0B);
  static const Color careerLaw = Color(0xFF7C3AED);
  static const Color careerEngineering = Color(0xFFEF4444);
  static const Color careerDesign = Color(0xFFEC4899);
  static const Color careerEducation = Color(0xFF06B6D4);
  static const Color careerFinance = Color(0xFF0D9488);

  // ─── Glassmorphism ───────────────────────────────────────────────────────
  static const Color glassWhite = Color(0xCCFFFFFF);
  static const Color glassBlue = Color(0x1A1A56DB);
  static const Color glassDark = Color(0x800B1D3A);

  // ─── Shimmer ─────────────────────────────────────────────────────────────
  static const Color shimmerBase = Color(0xFFE8EDF5);
  static const Color shimmerHighlight = Color(0xFFF8FAFF);

  // ─── Gradients (color stops) ──────────────────────────────────────────────
  static const Color gradientNavyStart = Color(0xFF0B1D3A);
  static const Color gradientNavyEnd = Color(0xFF1A3A6B);
  static const Color gradientBlueStart = Color(0xFF1A56DB);
  static const Color gradientBlueEnd = Color(0xFF06B6D4);
  static const Color gradientTealStart = Color(0xFF0D9488);
  static const Color gradientTealEnd = Color(0xFF06B6D4);
  static const Color gradientPremiumStart = Color(0xFF7C3AED);
  static const Color gradientPremiumEnd = Color(0xFFF59E0B);
}